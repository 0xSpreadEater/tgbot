"""V2 module stub. Airdrop farming opportunities detection planned
for a future phase. The airdrop_opportunities table exists in the
schema but no logic uses it yet. Reserved interface:

    class AirdropOpportunity: ...
    async def detect_opportunities(...) -> list[AirdropOpportunity]: ...
"""
